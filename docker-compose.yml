services:
  db:
    image: postgres:16
    container_name: fenmo_postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: expenses
    volumes:
      - fenmo_pgdata:/var/lib/postgresql/data

volumes:
  fenmo_pgdata:
